import React from "react";

const styleContainerHeader: React.CSSProperties = {
    background: '#024183'
}

const styleTitleHeader: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
    height: "60px",
}

const styleIconHeader: React.CSSProperties = {
    width: "30%",
    margin: "0 16px"
}

const styleIcon: React.CSSProperties = {
    fontSize: "24px",
    width: "32px",
    height: "32px",
    marginInline: "8px"
}

export {
    styleContainerHeader,
    styleTitleHeader,
    styleIconHeader,
    styleIcon
}
